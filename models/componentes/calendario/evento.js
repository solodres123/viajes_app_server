class Evento{

    constructor(inicio,fin,usuario,id,prioridad){
           this.inicio=inicio;
              this.fin=fin;
              this.usuario=usuario;
              this.id = id;
              this. prioridad = prioridad; 
    }
} 

module.exports=Evento;